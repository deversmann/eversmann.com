		<item>
			<title><?= $entry_info->title ?></title>
			<link><?= $blog_info->url."/".date(Y,$entry_info->date)."/".date(m,$entry_info->date)."/".date(d,$entry_info->date)."#".$entry_info->fn ?></link>
			<description>
	<?	foreach ($entry_info->lines as $line) {
			if (trim($line)) { ?>
				<p><?= $line ?></p>
	<?		}
		} 
	?>
			</description>
			<pubDate><?= date('r', $entry_info->date) ?></pubDate>
			<guid><?= $blog_info->url."/".date(Y,$entry_info->date)."/".date(m,$entry_info->date)."/".date(d,$entry_info->date)."#".$entry_info->fn ?></guid>
		</item>
